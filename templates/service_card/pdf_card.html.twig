<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carte de Service - {{ service.nom }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f5f5f5;
            padding: 20px;
        }

        .card-container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .card-header {
            background: linear-gradient(135deg, {{ service.couleur }} 0%, #1a5276 100%);
            color: white;
            padding: 40px 30px;
            position: relative;
            overflow: hidden;
        }

        .card-header::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 1px, transparent 1px);
            background-size: 50px 50px;
        }

        .card-header-content {
            position: relative;
            z-index: 1;
        }

        .card-logo {
            width: 80px;
            height: 80px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
            font-size: 40px;
            font-weight: bold;
        }

        .card-title {
            font-size: 32px;
            font-weight: 700;
            margin-bottom: 8px;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
        }

        .card-subtitle {
            font-size: 14px;
            opacity: 0.9;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .card-body {
            padding: 40px 30px;
        }

        .info-section {
            margin-bottom: 30px;
        }

        .info-section:last-child {
            margin-bottom: 0;
        }

        .section-title {
            font-size: 12px;
            font-weight: 700;
            color: {{ service.couleur }};
            text-transform: uppercase;
            letter-spacing: 1.5px;
            margin-bottom: 15px;
            padding-bottom: 8px;
            border-bottom: 2px solid {{ service.couleur }};
        }

        .info-row {
            display: flex;
            margin-bottom: 12px;
            align-items: flex-start;
        }

        .info-label {
            font-weight: 600;
            color: #2c3e50;
            width: 120px;
            flex-shrink: 0;
            font-size: 13px;
        }

        .info-value {
            color: #555;
            flex: 1;
            font-size: 13px;
            line-height: 1.5;
            word-break: break-word;
        }

        .info-value.highlight {
            color: {{ service.couleur }};
            font-weight: 600;
        }

        .card-footer {
            background: #f8f9fa;
            padding: 20px 30px;
            border-top: 1px solid #e9ecef;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 11px;
            color: #6c757d;
        }

        .footer-left {
            flex: 1;
        }

        .footer-right {
            text-align: right;
        }

        .generated-by {
            font-size: 10px;
            color: #adb5bd;
            margin-top: 5px;
        }

        .badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: 600;
            margin-right: 8px;
        }

        .badge-active {
            background: #d4edda;
            color: #155724;
        }

        .badge-inactive {
            background: #f8d7da;
            color: #721c24;
        }

        .detail-level-1 .section-title:nth-of-type(n+2) {
            display: none;
        }

        .detail-level-1 .info-section:nth-of-type(n+2) {
            display: none;
        }

        .detail-level-2 .section-title:nth-of-type(n+3) {
            display: none;
        }

        .detail-level-2 .info-section:nth-of-type(n+3) {
            display: none;
        }

        @media print {
            body {
                background: white;
                padding: 0;
            }

            .card-container {
                box-shadow: none;
                border-radius: 0;
            }
        }

        @page {
            size: A4;
            margin: 0;
        }
    </style>
</head>
<body>
    <div class="card-container detail-level-{{ detailLevel }}">
        <!-- En-tête -->
        <div class="card-header">
            <div class="card-header-content">
                {% if service.logo %}
                    <div class="card-logo">
                        <img src="{{ service.logo }}" alt="Logo" style="width: 100%; height: 100%; object-fit: contain;">
                    </div>
                {% else %}
                    <div class="card-logo">{{ service.code|first }}</div>
                {% endif %}
                <h1 class="card-title">{{ service.nom }}</h1>
                <p class="card-subtitle">{{ service.typeService ?? 'Service Hospitalier' }}</p>
            </div>
        </div>

        <!-- Corps -->
        <div class="card-body">
            <!-- Section Informations Générales -->
            <div class="info-section">
                <div class="section-title">Informations Générales</div>
                <div class="info-row">
                    <span class="info-label">Code:</span>
                    <span class="info-value highlight">{{ service.code }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Hôpital:</span>
                    <span class="info-value">{{ service.hopital }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Statut:</span>
                    <span class="info-value">
                        {% if service.actif %}
                            <span class="badge badge-active">Actif</span>
                        {% else %}
                            <span class="badge badge-inactive">Inactif</span>
                        {% endif %}
                    </span>
                </div>
            </div>

            <!-- Section Détails (niveau basique et plus) -->
            {% if detailLevel >= 1 %}
            <div class="info-section">
                <div class="section-title">Détails du Service</div>
                {% if service.description %}
                <div class="info-row">
                    <span class="info-label">Description:</span>
                    <span class="info-value">{{ service.description|truncate(150) }}</span>
                </div>
                {% endif %}
                {% if service.localisation %}
                <div class="info-row">
                    <span class="info-label">Localisation:</span>
                    <span class="info-value">{{ service.localisation }}</span>
                </div>
                {% endif %}
                {% if service.telephone %}
                <div class="info-row">
                    <span class="info-label">Téléphone:</span>
                    <span class="info-value">{{ service.telephone }}</span>
                </div>
                {% endif %}
            </div>
            {% endif %}

            <!-- Section Ressources (niveau intermédiaire et plus) -->
            {% if detailLevel >= 2 %}
            <div class="info-section">
                <div class="section-title">Ressources</div>
                {% if service.nombreLits %}
                <div class="info-row">
                    <span class="info-label">Nombre de Lits:</span>
                    <span class="info-value highlight">{{ service.nombreLits }}</span>
                </div>
                {% endif %}
                {% if service.email %}
                <div class="info-row">
                    <span class="info-label">Email:</span>
                    <span class="info-value">{{ service.email }}</span>
                </div>
                {% endif %}
                {% if service.chefServiceId %}
                <div class="info-row">
                    <span class="info-label">Chef de Service:</span>
                    <span class="info-value">ID: {{ service.chefServiceId }}</span>
                </div>
                {% endif %}
            </div>
            {% endif %}

            <!-- Section Administration (niveau complet) -->
            {% if detailLevel >= 3 %}
            <div class="info-section">
                <div class="section-title">Administration</div>
                <div class="info-row">
                    <span class="info-label">ID Service:</span>
                    <span class="info-value">{{ service.id }}</span>
                </div>
                {% if service.dateCreation %}
                <div class="info-row">
                    <span class="info-label">Créé le:</span>
                    <span class="info-value">{{ service.dateCreation|date('d/m/Y H:i') }}</span>
                </div>
                {% endif %}
                <div class="info-row">
                    <span class="info-label">Hôpital ID:</span>
                    <span class="info-value">{{ service.hopitalId }}</span>
                </div>
            </div>
            {% endif %}
        </div>

        <!-- Pied de page -->
        <div class="card-footer">
            <div class="footer-left">
                <strong>Généré par:</strong> {{ user.prenom }} {{ user.nom }}
                <div class="generated-by">{{ generatedAt|date('d/m/Y à H:i:s') }}</div>
            </div>
            <div class="footer-right">
                <strong>Rôle:</strong> {{ user.role }}
            </div>
        </div>
    </div>
</body>
</html>
