<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carte de Service - {{ service.nom }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: white;
            width: 1200px;
            height: 800px;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0;
        }

        .card {
            width: 100%;
            height: 100%;
            display: flex;
            background: white;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
        }

        .card-left {
            flex: 0 0 40%;
            background: linear-gradient(135deg, {{ service.couleur }} 0%, #1a5276 100%);
            color: white;
            padding: 60px 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .card-left::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 1px, transparent 1px);
            background-size: 50px 50px;
        }

        .card-left-content {
            position: relative;
            z-index: 1;
        }

        .card-logo {
            width: 120px;
            height: 120px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 30px;
            font-size: 60px;
            font-weight: bold;
            border: 3px solid rgba(255, 255, 255, 0.3);
        }

        .card-logo img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }

        .card-code {
            font-size: 14px;
            opacity: 0.9;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 15px;
        }

        .card-title {
            font-size: 36px;
            font-weight: 700;
            margin-bottom: 10px;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
        }

        .card-type {
            font-size: 14px;
            opacity: 0.85;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .card-right {
            flex: 1;
            padding: 60px 50px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .info-block {
            margin-bottom: 40px;
        }

        .info-block:last-child {
            margin-bottom: 0;
        }

        .block-title {
            font-size: 12px;
            font-weight: 700;
            color: {{ service.couleur }};
            text-transform: uppercase;
            letter-spacing: 1.5px;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid {{ service.couleur }};
        }

        .info-item {
            margin-bottom: 12px;
            display: flex;
            align-items: flex-start;
        }

        .info-item:last-child {
            margin-bottom: 0;
        }

        .info-label {
            font-weight: 600;
            color: #2c3e50;
            width: 140px;
            flex-shrink: 0;
            font-size: 13px;
        }

        .info-value {
            color: #555;
            flex: 1;
            font-size: 13px;
            line-height: 1.4;
        }

        .info-value.highlight {
            color: {{ service.couleur }};
            font-weight: 600;
        }

        .badge {
            display: inline-block;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 11px;
            font-weight: 600;
        }

        .badge-active {
            background: #d4edda;
            color: #155724;
        }

        .badge-inactive {
            background: #f8d7da;
            color: #721c24;
        }

        .footer-info {
            font-size: 11px;
            color: #999;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #e9ecef;
        }

        .hide-level-1 .block-title:nth-of-type(n+2),
        .hide-level-1 .info-block:nth-of-type(n+2) {
            display: none;
        }

        .hide-level-2 .block-title:nth-of-type(n+3),
        .hide-level-2 .info-block:nth-of-type(n+3) {
            display: none;
        }
    </style>
</head>
<body>
    <div class="card {% if detailLevel == 1 %}hide-level-1{% elseif detailLevel == 2 %}hide-level-2{% endif %}">
        <!-- Côté gauche -->
        <div class="card-left">
            <div class="card-left-content">
                {% if service.logo %}
                    <div class="card-logo">
                        <img src="{{ service.logo }}" alt="Logo">
                    </div>
                {% else %}
                    <div class="card-logo">{{ service.code|first }}</div>
                {% endif %}
                <div class="card-code">{{ service.code }}</div>
                <h1 class="card-title">{{ service.nom }}</h1>
                <p class="card-type">{{ service.typeService ?? 'Service Hospitalier' }}</p>
            </div>
        </div>

        <!-- Côté droit -->
        <div class="card-right">
            <!-- Informations Générales -->
            <div class="info-block">
                <div class="block-title">Informations Générales</div>
                <div class="info-item">
                    <span class="info-label">Hôpital:</span>
                    <span class="info-value">{{ service.hopital }}</span>
                </div>
                <div class="info-item">
                    <span class="info-label">Statut:</span>
                    <span class="info-value">
                        {% if service.actif %}
                            <span class="badge badge-active">Actif</span>
                        {% else %}
                            <span class="badge badge-inactive">Inactif</span>
                        {% endif %}
                    </span>
                </div>
            </div>

            <!-- Détails (niveau basique et plus) -->
            {% if detailLevel >= 1 %}
            <div class="info-block">
                <div class="block-title">Détails</div>
                {% if service.localisation %}
                <div class="info-item">
                    <span class="info-label">Localisation:</span>
                    <span class="info-value">{{ service.localisation }}</span>
                </div>
                {% endif %}
                {% if service.telephone %}
                <div class="info-item">
                    <span class="info-label">Téléphone:</span>
                    <span class="info-value">{{ service.telephone }}</span>
                </div>
                {% endif %}
            </div>
            {% endif %}

            <!-- Ressources (niveau intermédiaire et plus) -->
            {% if detailLevel >= 2 %}
            <div class="info-block">
                <div class="block-title">Ressources</div>
                {% if service.nombreLits %}
                <div class="info-item">
                    <span class="info-label">Nombre de Lits:</span>
                    <span class="info-value highlight">{{ service.nombreLits }}</span>
                </div>
                {% endif %}
                {% if service.email %}
                <div class="info-item">
                    <span class="info-label">Email:</span>
                    <span class="info-value">{{ service.email }}</span>
                </div>
                {% endif %}
            </div>
            {% endif %}

            <!-- Pied de page -->
            <div class="footer-info">
                <strong>Généré par:</strong> {{ user.prenom }} {{ user.nom }} ({{ user.role }})<br>
                <strong>Date:</strong> {{ generatedAt|date('d/m/Y H:i:s') }}
            </div>
        </div>
    </div>
</body>
</html>
